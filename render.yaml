services:
  - type: web
    name: quiz-app
    runtime: dotnet
    repo: https://github.com/Mitchap/Quiz-App
    buildCommand: dotnet publish -c Release -o out
    startCommand: dotnet out/"Quiz App.dll"
    branch: main
